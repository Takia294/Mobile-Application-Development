name: organ_and_blood_donation
description: "A new Flutter project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dropdown_search: ^5.0.6
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  file_picker: ^8.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  path_provider: ^2.1.3

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/screens/logo.png